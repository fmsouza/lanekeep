[package]
name = "lanekeep-server"
description = "LSP and MCP servers for lanekeep, over stdio."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
keywords.workspace = true
categories.workspace = true

[dependencies]
# Protocol only. The engine wiring lives in the CLI, which passes a closure — so this crate
# never learns how a check is run, and its tests need no project on disk.
lanekeep-core = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

[lints]
workspace = true

# This crate is its own workspace, so the repository root's anchored `/target/` rule does
# not reach it.
/target

# Regenerated by `cargo component build` on every build, from wit/spike.wit. Committing it
# would produce a diff every time the toolchain's bindgen output changes, for a file nobody
# edits.
/src/bindings.rs

# Anchored literal paths only, matching `rust-rules/.gitignore`'s own convention.

# Regenerated by `cargo component build` on every build, from
# ../../crates/lanekeep-wasm/wit/world.wit. Committing it would produce a diff every time the
# toolchain's bindgen output changes, for a file nobody edits.
/src/bindings.rs
